{
  "scripts": {
    "client": "cd client && yarn run start",
    "server": "cd server && nodemon server.js",
    "start-server": "cd server && npm install && nodemon server.js",
    "start-client": "cd client && npm install && npm run start"
  },
  "proxy": "http://localhost:3001",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {}
}
